const useStyles = () => ({
  shopTitle: {
    display: "flex",
    justifyContent: "center",
  },
  products: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
  },
});

export default useStyles;
